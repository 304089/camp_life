= render "layouts/menu"
  .user_show_area
    - if @user == current_user
      .profile_area
        = link_to edit_user_path(@user)
          = render "profile", user: @user
    - else
      .profile_area
        = render "profile", user: @user
    = link_to "ログアウト", logout_path
= render "profile", user: @user
